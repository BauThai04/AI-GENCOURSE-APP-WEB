import 'package:flutter/material.dart';

class AiGenCourseScreen extends StatelessWidget {
  const AiGenCourseScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("AI GenCourse Screen")));
  }
}
